# Wallet REST API

Тестовое задание на позицию Python Developer. Приложение для управления балансом кошельков с поддержкой конкурентных запросов.

## Стек технологий
* **Python 3.12**
* **FastAPI** — веб-фреймворк
* **PostgreSQL 16** — база данных
* **SQLAlchemy 2.0** — ORM (асинхронный режим)
* **Alembic** — миграции БД
* **uv** — современный менеджер пакетов и окружения
* **Docker & Docker Compose** — контейнеризация

## Особенности реализации
1. **Конкурентность**: Использована стратегия **Pessimistic Locking** (`SELECT FOR UPDATE`) для предотвращения Race Condition при одновременных запросах к одному кошельку.
2. **Целостность данных**: На уровне БД добавлена проверка `CheckConstraint("balance >= 0")`, исключающая отрицательный баланс.
3. **Точность**: Все расчеты ведутся с использованием типа `Decimal`, что критически важно для финансовых операций.
4. **Валидация**: Входящие данные строго валидируются схемами Pydantic (типы данных, положительные суммы).

## Запуск проекта

### 1. Подготовка
Клонируйте репозиторий:
```bash
git clone https://github.com
cd ВАШ_РЕПОЗИТОРИЙ
```

Создайте файл `.env` из примера:
```bash
cp .env.example .env
```

### 2. Запуск через Docker Compose
Всё приложение (API + DB) поднимается одной командой:
```bash
docker-compose up --build
```
*Миграции применятся автоматически при старте контейнера.*

## Использование API

После запуска API будет доступно по адресу: `http://localhost:8000`

* **Swagger UI (Интерактивная документация)**: [http://localhost:8000/docs](http://localhost:8000/docs)
* **Redoc**: [http://localhost:8000/redoc](http://localhost:8000/redoc)

### Основные эндпоинты:
* `POST /api/v1/wallets/` — создать новый кошелек.
* `GET /api/v1/wallets/{WALLET_UUID}` — получить баланс кошелька.
* `POST /api/v1/wallets/{WALLET_UUID}/operation` — выполнить операцию (DEPOSIT или WITHDRAW).

## Тестирование
Для запуска тестов (внутри контейнера или локально):
```bash
pytest
```
